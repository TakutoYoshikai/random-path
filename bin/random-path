#!/usr/bin/env node

const RandomPath = require("../index");
let dir = "/";
if (process.argv.length >= 3) {
  dir = process.argv[2];
}
const path = RandomPath(dir);
if (path) {
  console.log(path);
}
